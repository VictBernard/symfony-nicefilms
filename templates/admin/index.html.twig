{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}

    {% for message in app.flashes('success') %}
    <div class="alert alert-success">
        {{ message }}
    </div>
    {% endfor %}

    <div class="flex flex-row justify-center m-10 flex-wrap">
        <button class="bg-transparent hover:bg-blue-500 text-blue-700 font-semibold hover:text-white py-2 px-4 border border-blue-500 hover:border-transparent rounded m-5">
            <a class="text-white text-center text-2xl m-2" href="{{ path('app_user_index') }}">Liste des utilisateurs</a>
        </button>
        <button class="bg-transparent hover:bg-blue-500 text-blue-700 font-semibold hover:text-white py-2 px-4 border border-blue-500 hover:border-transparent rounded m-5">
<<<<<<< HEAD
            <a class="text-white text-center text-2xl m-2" href="{{ path('app_randomUsers') }}">Génerer 50 utilisateurs aléatoires</a>
        </button>      
        <button class="bg-transparent hover:bg-blue-500 text-blue-700 font-semibold hover:text-white py-2 px-4 border border-blue-500 hover:border-transparent rounded m-5">
            <a class="text-white text-center text-2xl m-2" href="{{ path('app_randomReviews') }}">Génerer 50 avis aléatoires sur 25 séries aléatoires</a>
=======
            <a class="text-white text-center text-2xl m-2" href="{{ path('app_randomUsers') }}">Génerer 30 utilisateurs aléatoires</a>
>>>>>>> adedb8baf (push before merging main into our branch)
        </button>       
    </div>
{% endblock %}
